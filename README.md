# sails-nodejs-app
framework that provides true Rails‑style CRUD scaffolding (controllers + models + views + routes auto‑generated) is Sails.js, using its built‑in blueprints + scaffolding generators. Sails = Rails for Node.js
